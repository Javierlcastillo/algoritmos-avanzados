#include <iostream>
#include <vector>
using namespace std;

vector<int> solucion(string s){
    int n = s.size();
    vector <int> dp(n,0);
    int count = 0;
    int final = n;
    for (int i = 0; i < n; i++){
        if(s[i] = s[final])
    }
    return dp;
}

int main(){
    string s; 
    cin >> s; 
    vector<int> resultado = solucion(s); 
}

// abcababcab
